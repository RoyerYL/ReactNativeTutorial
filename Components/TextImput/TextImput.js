import { StyleSheet, View, Pressable, Text } from 'react-native';
import FontAwesome from "@expo/vector-icons/FontAwesome";

export default function TextImput({ text }) {

    return (
        <View >
            <Text >{text}</Text>
        </View>
    );
}

const styles = StyleSheet.create({

});
